import styled from 'styled-components'

// TODO: Remove once `PageHeader` is properly styled
const PageHeaderContent = styled.div`
  width: 100%;
  display: flex;
  flex-direction: row;
  gap: 10px;
  justify-content: flex-end;
  margin-right: 108px;
  position: relative;
  white-space: nowrap;
`

export default PageHeaderContent
